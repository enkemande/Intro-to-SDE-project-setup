const myArray = ["Beans", "Rice", "Vegetables"]; // Array with 3 items stored in variable
myArray[0] = "Eggs"; // Modifying first item of array using bracket & assignment
myArray[1] = "potatoes"; // Modifying second item of array using bracket & assignment

console.log(myArray);

// Adding a new item to the beginning of the array
myArray.unshift("Yams");

console.log(myArray);